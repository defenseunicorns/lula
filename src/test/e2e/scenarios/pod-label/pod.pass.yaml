#kics-scan ignore
---
apiVersion: v1
kind: Pod
metadata:
  name: test-pod-label
  namespace: validation-test
  labels: 
    foo: bar
    lula: "true"
spec:
  containers:
    - image: nginx
      name: nginx
