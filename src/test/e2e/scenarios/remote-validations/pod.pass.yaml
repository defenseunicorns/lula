#kics-scan ignore
---
apiVersion: v1
kind: Pod
metadata:
  name: remote-validations-pod
  namespace: validation-test
  labels:
    foo: bar
    lula: "true"
spec:
  containers:
    - image: nginx
      name: pods-simple-container
